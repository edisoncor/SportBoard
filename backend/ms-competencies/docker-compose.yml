version: '3.8'

services:
  competencies:
    build:
      context: .
    container_name: competencies
    ports:
      - "8010:8010"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - sportboard-network

networks:
  sportboard-network:
    external: true